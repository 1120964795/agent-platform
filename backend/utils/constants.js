// utils/constants.js

// 定义默认模型名称
const DEFAULT_MODEL_NAME = 'DeepSeek';

// 定义接口请求超时时间 (模拟)
const REQUEST_TIMEOUT_MS = 8000;

// 定义统一的错误提示信息
const ERROR_MSG_EMPTY_INPUT = '发送的内容不能为空，请输入有效信息。';
const ERROR_MSG_NETWORK_FAIL = '模型调用失败或网络超时，请稍后重试。';